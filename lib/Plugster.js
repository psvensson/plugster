// Generated by CoffeeScript 1.9.2
(function() {
  var Plugster, promise, uuid;

  uuid = require('node-uuid');

  promise = require('node-promise');

  Plugster = (function() {
    Plugster.modules = [];

    function Plugster(moduleName1, ports) {
      this.moduleName = moduleName1;
      this.input = ports.input;
      this.output = {};
      ports.output.forEach(function(portName) {
        return this.output[portName] = void 0;
      });
      Plugster.modules[moduleName] = this;
    }

    Plugster.wire = function(outputModule, outputPortName, inputModule, inputPortName) {
      console.log('Plugster::wire wiring module "' + outputModule.name + " port " + outputPortName + '" to module "' + inputModule.name + '" port "' + outputPortName);
      return outputModule.output[outputPortName] = inputModule.input[inputPortName];
    };

    Plugster.prototype.isWiredTo = function(outputPortName, inputModule, inputPortname) {
      return this.output[outputPortName] === inputModule.input[inputPortName];
    };

    Plugster.prototype.send = function(outputPortName, data) {};

    return Plugster;

  })();

  module.exports = Plugster;

}).call(this);

//# sourceMappingURL=Plugster.js.map
